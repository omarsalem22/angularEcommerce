import { CartHighlightDirective } from './cart-highlight.directive';

describe('CartHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CartHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
